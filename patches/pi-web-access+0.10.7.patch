diff --git a/node_modules/pi-web-access/README.md b/node_modules/pi-web-access/README.md
index b3bd267..50c97a5 100644
--- a/node_modules/pi-web-access/README.md
+++ b/node_modules/pi-web-access/README.md
@@ -28,7 +28,7 @@ https://github.com/user-attachments/assets/cac6a17a-1eeb-4dde-9818-cdf85d8ea98f
 pi install npm:pi-web-access
 ```
 
-Works immediately with no API keys — Exa MCP provides zero-config search. For more providers or direct API access, add keys to `~/.pi/web-search.json`:
+Works immediately with no API keys — Exa MCP provides zero-config search. For more providers or direct API access, add keys to `~/.linx/pi-web-access.json`:
 
 ```json
 {
@@ -229,7 +229,7 @@ Toggle or configure the curator workflow at runtime.
 /curator summary-review     # explicit workflow
 ```
 
-Persists to `~/.pi/web-search.json` and takes effect on the next `web_search` call. When disabled, `web_search` returns raw results without opening the curator window.
+Persists to `~/.linx/pi-web-access.json` and takes effect on the next `web_search` call. When disabled, `web_search` returns raw results without opening the curator window.
 
 ### /search
 
@@ -253,7 +253,7 @@ Toggle with **Ctrl+Shift+W** to see live request/response activity:
 
 ## Configuration
 
-All config lives in `~/.pi/web-search.json`. Every field is optional.
+All config lives in `~/.linx/pi-web-access.json`. Every field is optional.
 
 ```json
 {
@@ -293,7 +293,7 @@ All config lives in `~/.pi/web-search.json`. Every field is optional.
 
 ### Shortcuts
 
-Both shortcuts are configurable via `~/.pi/web-search.json`:
+Both shortcuts are configurable via `~/.linx/pi-web-access.json`:
 
 ```json
 {
diff --git a/node_modules/pi-web-access/exa.ts b/node_modules/pi-web-access/exa.ts
index bf09382..b0e51e0 100644
--- a/node_modules/pi-web-access/exa.ts
+++ b/node_modules/pi-web-access/exa.ts
@@ -8,8 +8,8 @@ import type { SearchOptions, SearchResponse } from "./perplexity.js";
 const EXA_ANSWER_URL = "https://api.exa.ai/answer";
 const EXA_SEARCH_URL = "https://api.exa.ai/search";
 const EXA_MCP_URL = "https://mcp.exa.ai/mcp";
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
-const USAGE_PATH = join(homedir(), ".pi", "exa-usage.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
+const USAGE_PATH = join(homedir(), ".linx", "pi-web-access-exa-usage.json");
 
 const MONTHLY_LIMIT = 1000;
 const WARNING_THRESHOLD = 800;
@@ -115,7 +115,7 @@ function readUsage(): ExaUsage {
 }
 
 function writeUsage(usage: ExaUsage): void {
-	const dir = join(homedir(), ".pi");
+	const dir = join(homedir(), ".linx");
 	if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
 	writeFileSync(USAGE_PATH, JSON.stringify(usage, null, 2) + "\n");
 }
diff --git a/node_modules/pi-web-access/extract.ts b/node_modules/pi-web-access/extract.ts
index d1c0b2c..271b963 100644
--- a/node_modules/pi-web-access/extract.ts
+++ b/node_modules/pi-web-access/extract.ts
@@ -356,7 +356,7 @@ export async function extractContent(
 		try {
 			const result = await extractVideo(localVideo.info, signal, options);
 			if (signal?.aborted) return abortedResult(url);
-			return result ?? { url, title: "", content: "", error: "Video analysis requires Gemini access. Either:\n  1. Sign into gemini.google.com in Chrome (free, uses cookies)\n  2. Set GEMINI_API_KEY in ~/.pi/web-search.json" };
+			return result ?? { url, title: "", content: "", error: "Video analysis requires Gemini access. Either:\n  1. Sign into gemini.google.com in Chrome (free, uses cookies)\n  2. Set GEMINI_API_KEY in ~/.linx/pi-web-access.json" };
 		} catch (err) {
 			if (isAbortError(err)) return abortedResult(url);
 			return { url, title: "", content: "", error: errorMessage(err) };
@@ -438,7 +438,7 @@ export async function extractContent(
 		httpResult.error,
 		"",
 		"Fallback options:",
-		"  \u2022 Set GEMINI_API_KEY in ~/.pi/web-search.json",
+		"  \u2022 Set GEMINI_API_KEY in ~/.linx/pi-web-access.json",
 		"  \u2022 Sign into gemini.google.com in Chrome",
 		"  \u2022 Use web_search to find content about this topic",
 	].join("\n");
diff --git a/node_modules/pi-web-access/gemini-api.ts b/node_modules/pi-web-access/gemini-api.ts
index f56fe48..7e8eca4 100644
--- a/node_modules/pi-web-access/gemini-api.ts
+++ b/node_modules/pi-web-access/gemini-api.ts
@@ -3,7 +3,7 @@ import { homedir } from "node:os";
 import { join } from "node:path";
 
 export const API_BASE = "https://generativelanguage.googleapis.com/v1beta";
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 export const DEFAULT_MODEL = "gemini-3-flash-preview";
 
 interface GeminiApiConfig {
diff --git a/node_modules/pi-web-access/gemini-search.ts b/node_modules/pi-web-access/gemini-search.ts
index 57ccdfa..bbbafb5 100644
--- a/node_modules/pi-web-access/gemini-search.ts
+++ b/node_modules/pi-web-access/gemini-search.ts
@@ -14,7 +14,7 @@ export interface AttributedSearchResponse extends SearchResponse {
 	provider: ResolvedSearchProvider;
 }
 
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 let cachedSearchConfig: { searchProvider: SearchProvider; searchModel?: string } | null = null;
 
@@ -119,7 +119,7 @@ export async function search(query: string, options: FullSearchOptions = {}): Pr
 		if (result) return { ...result, provider: "gemini" };
 		throw new Error(
 			"Gemini search unavailable. Either:\n" +
-			"  1. Set GEMINI_API_KEY in ~/.pi/web-search.json\n" +
+			"  1. Set GEMINI_API_KEY in ~/.linx/pi-web-access.json\n" +
 			"  2. Sign into gemini.google.com in a supported Chromium-based browser"
 		);
 	}
@@ -182,9 +182,9 @@ export async function search(query: string, options: FullSearchOptions = {}): Pr
 
 	throw new Error(
 		"No search provider available. Either:\n" +
-		"  1. Set perplexityApiKey in ~/.pi/web-search.json\n" +
-		"  2. Set EXA_API_KEY (or exaApiKey) in ~/.pi/web-search.json\n" +
-		"  3. Set GEMINI_API_KEY in ~/.pi/web-search.json\n" +
+		"  1. Set perplexityApiKey in ~/.linx/pi-web-access.json\n" +
+		"  2. Set EXA_API_KEY (or exaApiKey) in ~/.linx/pi-web-access.json\n" +
+		"  3. Set GEMINI_API_KEY in ~/.linx/pi-web-access.json\n" +
 		"  4. Sign into gemini.google.com in a supported Chromium-based browser"
 	);
 }
diff --git a/node_modules/pi-web-access/gemini-web-config.ts b/node_modules/pi-web-access/gemini-web-config.ts
index 77f7a09..0e50659 100644
--- a/node_modules/pi-web-access/gemini-web-config.ts
+++ b/node_modules/pi-web-access/gemini-web-config.ts
@@ -2,7 +2,7 @@ import { existsSync, readFileSync } from "node:fs";
 import { homedir } from "node:os";
 import { join } from "node:path";
 
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 interface GeminiWebConfig {
 	chromeProfile?: string;
diff --git a/node_modules/pi-web-access/github-extract.ts b/node_modules/pi-web-access/github-extract.ts
index 1e57e1a..c0a6bd1 100644
--- a/node_modules/pi-web-access/github-extract.ts
+++ b/node_modules/pi-web-access/github-extract.ts
@@ -6,7 +6,7 @@ import { activityMonitor } from "./activity.js";
 import type { ExtractedContent } from "./extract.js";
 import { checkGhAvailable, checkRepoSize, fetchViaApi, showGhHint } from "./github-api.js";
 
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 const BINARY_EXTENSIONS = new Set([
 	".png", ".jpg", ".jpeg", ".gif", ".bmp", ".ico", ".webp", ".svg", ".tiff", ".tif",
diff --git a/node_modules/pi-web-access/index.ts b/node_modules/pi-web-access/index.ts
index aa30987..f06bb9f 100644
--- a/node_modules/pi-web-access/index.ts
+++ b/node_modules/pi-web-access/index.ts
@@ -1,7 +1,7 @@
-import type { ExtensionAPI, ExtensionContext } from "@mariozechner/pi-coding-agent";
-import { Box, Text, truncateToWidth } from "@mariozechner/pi-tui";
+import type { ExtensionAPI, ExtensionContext } from "@earendil-works/pi-coding-agent";
+import { Box, Text, truncateToWidth } from "@earendil-works/pi-tui";
 import { Type } from "typebox";
-import { StringEnum, complete, getModel, type Model } from "@mariozechner/pi-ai";
+import { StringEnum, complete, getModel, type Model } from "@earendil-works/pi-ai";
 import { fetchAllContent, type ExtractedContent } from "./extract.js";
 import { clearCloneCache } from "./github-extract.js";
 import { search, type SearchProvider, type ResolvedSearchProvider } from "./gemini-search.js";
@@ -39,7 +39,7 @@ import { isGeminiApiAvailable } from "./gemini-api.js";
 import { getActiveGoogleEmail, isGeminiWebAvailable } from "./gemini-web.js";
 import { isBrowserCookieAccessAllowed } from "./gemini-web-config.ts";
 
-const WEB_SEARCH_CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const WEB_SEARCH_CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 interface WebSearchConfig {
 	provider?: string;
@@ -91,7 +91,7 @@ function saveConfig(updates: Partial<WebSearchConfig>): void {
 	}
 
 	Object.assign(config, updates);
-	const dir = join(homedir(), ".pi");
+	const dir = join(homedir(), ".linx");
 	if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
 	writeFileSync(WEB_SEARCH_CONFIG_PATH, JSON.stringify(config, null, 2) + "\n");
 }
@@ -2246,7 +2246,7 @@ export default function (pi: ExtensionAPI) {
 			if (!isBrowserCookieAccessAllowed()) {
 				pi.sendMessage({
 					customType: "google-account",
-					content: [{ type: "text", text: "Gemini Web browser cookie access is disabled. Set allowBrowserCookies: true in ~/.pi/web-search.json to enable it." }],
+					content: [{ type: "text", text: "Gemini Web browser cookie access is disabled. Set allowBrowserCookies: true in ~/.linx/pi-web-access.json to enable it." }],
 					display: "tool",
 					details: { available: false, cookieAccessAllowed: false },
 				}, { triggerTurn: true, deliverAs: "followUp" });
diff --git a/node_modules/pi-web-access/perplexity.ts b/node_modules/pi-web-access/perplexity.ts
index 628aeb3..57c673a 100644
--- a/node_modules/pi-web-access/perplexity.ts
+++ b/node_modules/pi-web-access/perplexity.ts
@@ -5,7 +5,7 @@ import { activityMonitor } from "./activity.js";
 import type { ExtractedContent } from "./extract.js";
 
 const PERPLEXITY_API_URL = "https://api.perplexity.ai/chat/completions";
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 const RATE_LIMIT = {
 	maxRequests: 10,
diff --git a/node_modules/pi-web-access/storage.ts b/node_modules/pi-web-access/storage.ts
index cf3eacc..4926425 100644
--- a/node_modules/pi-web-access/storage.ts
+++ b/node_modules/pi-web-access/storage.ts
@@ -1,4 +1,4 @@
-import type { ExtensionContext } from "@mariozechner/pi-coding-agent";
+import type { ExtensionContext } from "@earendil-works/pi-coding-agent";
 import type { ExtractedContent } from "./extract.js";
 import type { SearchResult } from "./perplexity.js";
 
diff --git a/node_modules/pi-web-access/summary-review.ts b/node_modules/pi-web-access/summary-review.ts
index 6a6c736..fc6bba6 100644
--- a/node_modules/pi-web-access/summary-review.ts
+++ b/node_modules/pi-web-access/summary-review.ts
@@ -1,5 +1,5 @@
-import { complete, getModel, type Message, type Model } from "@mariozechner/pi-ai";
-import type { ExtensionContext } from "@mariozechner/pi-coding-agent";
+import { complete, getModel, type Message, type Model } from "@earendil-works/pi-ai";
+import type { ExtensionContext } from "@earendil-works/pi-coding-agent";
 import type { QueryResultData } from "./storage.js";
 
 const PREFERRED_SUMMARY_MODELS = [
diff --git a/node_modules/pi-web-access/test/gemini-web-cookie-opt-in.test.mjs b/node_modules/pi-web-access/test/gemini-web-cookie-opt-in.test.mjs
index 6be5e59..e995591 100644
--- a/node_modules/pi-web-access/test/gemini-web-cookie-opt-in.test.mjs
+++ b/node_modules/pi-web-access/test/gemini-web-cookie-opt-in.test.mjs
@@ -27,8 +27,8 @@ test("browser cookie access is disabled unless explicitly allowed", async () =>
 	assert.equal(child.status, 0, child.stderr);
 	assert.equal(child.stdout.trim(), "false");
 
-	await mkdir(join(home, ".pi"), { recursive: true });
-	await writeFile(join(home, ".pi", "web-search.json"), JSON.stringify({ allowBrowserCookies: true }) + "\n", "utf8");
+	await mkdir(join(home, ".linx"), { recursive: true });
+	await writeFile(join(home, ".linx", "pi-web-access.json"), JSON.stringify({ allowBrowserCookies: true }) + "\n", "utf8");
 
 	child = runCookieAccessCheck(home);
 	assert.equal(child.status, 0, child.stderr);
diff --git a/node_modules/pi-web-access/video-extract.ts b/node_modules/pi-web-access/video-extract.ts
index 8277e19..9f3027e 100644
--- a/node_modules/pi-web-access/video-extract.ts
+++ b/node_modules/pi-web-access/video-extract.ts
@@ -9,7 +9,7 @@ import { queryGeminiApiWithVideo, getApiKey, API_BASE } from "./gemini-api.js";
 import { extractHeadingTitle, type ExtractedContent, type ExtractOptions, type FrameResult } from "./extract.js";
 import { readExecError, trimErrorText, mapFfmpegError } from "./utils.js";
 
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 const UPLOAD_BASE = "https://generativelanguage.googleapis.com/upload/v1beta";
 
 const DEFAULT_VIDEO_PROMPT = `Extract the complete content of this video. Include:
diff --git a/node_modules/pi-web-access/youtube-extract.ts b/node_modules/pi-web-access/youtube-extract.ts
index a34cd76..817b95a 100644
--- a/node_modules/pi-web-access/youtube-extract.ts
+++ b/node_modules/pi-web-access/youtube-extract.ts
@@ -9,7 +9,7 @@ import { searchWithPerplexity } from "./perplexity.js";
 import { extractHeadingTitle, type ExtractedContent, type FrameResult, type VideoFrame } from "./extract.js";
 import { formatSeconds, readExecError, isTimeoutError, trimErrorText, mapFfmpegError } from "./utils.js";
 
-const CONFIG_PATH = join(homedir(), ".pi", "web-search.json");
+const CONFIG_PATH = join(homedir(), ".linx", "pi-web-access.json");
 
 const YOUTUBE_PROMPT = `Extract the complete content of this YouTube video. Include:
 1. Video title, channel name, and duration
