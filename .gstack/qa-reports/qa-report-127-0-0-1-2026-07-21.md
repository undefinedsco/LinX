# LinX Chat / Model Management QA — 2026-07-21

## Environments

- Persistent developer environment: LinX `http://127.0.0.1:5173`, Docker `xpod-local` on port `5737`, authenticated account `cuilinsu`.
- Isolated E2E environment: self-booted real xpod, real OIDC login, routed OpenAI-compatible provider.
- Persistent fixture prefix: `QA-PERSIST-20260721-*` (intentionally retained in Docker xpod).

## Executed flows

1. Verified Docker xpod health and restored the authenticated LinX Web session.
2. Opened Model Management, loaded the persisted provider and its ten synced models.
3. Exercised provider verification, model synchronization, provider selection, and model fuzzy search.
4. Created `QA-PERSIST-20260721-CHAT` with a default Thread and Pod workspace in the persistent xpod.
5. Queried xpod storage evidence and confirmed the Chat, Agent, Contact, Thread, and workspace relation were written.
6. Ran the isolated real-xpod E2E: login → provider creation → model sync → assistant creation → assistant visible after reload → model routing → streaming user/assistant messages → SPARQL evidence → refresh restoration → mobile layout.
7. Ran the Pi format matrix on desktop and mobile: thinking, text, tool call/result, Markdown headings/lists/links/code/table/quote/math, streaming/running/error/approval/plan states, image/file/source artifacts, unsafe-link handling, and overflow checks.

## Persisted evidence

- Developer xpod Chat: `QA-PERSIST-20260721-CHAT`.
- Developer xpod Chat subject: `http://localhost:5737/cuilinsu/.data/chat/dae57aa5-ab90-49a7-849e-21de3610fea6/index.ttl#this`.
- Developer xpod Thread subject: `http://localhost:5737/cuilinsu/.data/chat/dae57aa5-ab90-49a7-849e-21de3610fea6/index.ttl#f7577919-ced8-4926-808c-7e1f8ac6c375`.
- Latest isolated-run evidence: `chat-full-flow-evidence.json` in this directory.
- Screenshots: `screenshots/chat-full-flow-stream-complete.png`, `screenshots/chat-full-flow-mobile-restored.png`, `screenshots/pi-message-formats-desktop.png`, and `screenshots/pi-message-formats-mobile.png`.

## Results

- Real xpod full Chat flow: 1/1 passed, including new Assistant visibility after reload.
- Pi format matrix: 1/1 passed.
- Combined Chat E2E: 2/2 passed.
- Chat unit/component suite: 32 files, 222 tests passed.
- Production Web build: passed.
- Regression added: Pod chats are merged with the staged Secretary while welcome persistence is still in flight.
- Evidence JSON is now emitted on every full-flow E2E run and attached to Playwright output.

## Open defects found in the persistent developer Pod

1. `QA-PERSIST-20260721-CHAT` is physically present in xpod but is not visible in the current developer Chat list after reload. The persistent Pod contains six Chat resources, while the Web list falls back to only `AI Secretary`. The isolated clean-xpod assistant-create/reload scenario passes, so this is specific to the accumulated developer Pod/query state rather than the create flow.
2. The persistent provider's model sync succeeds, but the latest verification attempt changed its UI state to `未验证`; do not treat the earlier `可用` display as a successful current credential verification.
3. Searching `5.5` cannot return a result from the persisted provider because its synchronized list currently contains no 5.5 model. The fuzzy-search behavior remains covered separately by component/E2E fixtures.
4. xpod reports repeated values for Secretary single-value fields after repeated bootstraps. This data-integrity issue is a likely contributor to the developer Pod list-query fallback and needs a dedicated idempotent bootstrap cleanup/fix; the QA data was not deleted in this run.
